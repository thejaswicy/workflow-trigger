name: Secondary Workflow

on:
  workflow_run:
    workflows: ["Primary Workflow"]
    types:
      - completed

jobs:
  build:
    if: github.event.workflow_run.conclusion == 'success' && contains(github.event.workflow_run.head_commit.message, '[demo-trigger]')
    runs-on: ubuntu-latest
    steps:
      - name: Your BBB Workflow Steps
        # Add your steps here